---
status: idea
type: feature
priority: low
area: backend
---

Self-hosting / Federation Options

- self-host with low effort
  - linux VM
  - users with own namespace (and login != displayname)

- storage backends
  - local FS (filesystem, NAS, NFS,...)
  - remote bulk storage with server side buffer
    - originals in bulk, configurable high-res buffer (x MB, 1080p, 1440p, 2160p,..)
  - remote p2p storage backend (share storage?)
  - per-photo, (per user?) file encryption with local validation

  - storage reports

- Social feeds
  - let users define 'feeds' (public labels? subscription groups?)
  - let users browse/subscribe to feeds

  - activitypub?
